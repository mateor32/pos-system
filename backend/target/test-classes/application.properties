# ── Base de datos en memoria para tests (no requiere PostgreSQL) ──
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# Desactivar dialecto PostgreSQL explícito para evitar warning
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# JWT (igual que producción)
jwt.secret=pos-system-super-secret-key-2024-must-be-256-bits-long-enough
jwt.expiration=86400000
